# Basic Discord Bot Template

This template will allow you to plug in a persona and launch a discord bot using your Discord Bot Token and your OpenAI API key.

ENV toggles (variables, secrets) for everything make tuning easy without re-writing code.
